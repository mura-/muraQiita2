= form_for @comment, url: tip_comments_path(params[:id]) do |f|
  .errors
    %ul
      - @comment.errors.full_messages.each do |message|
        %li= message
  .field
    = f.label :content
    = f.text_area :content
  .actions
    = f.submit

